# Spec NNNN — <feature title>

- **Status:** draft | approved | in-progress | done | superseded
- **Feature ID:** F<n> (cross-ref `docs/SPEC.md` §4)
- **Author / date:** <name> — YYYY-MM-DD

## 1. Problem / motivation

What user need does this address? Why now? Link the backlog item in
`docs/IMPROVEMENTS.md` if any.

## 2. Goals

- Bullet list of what success looks like (observable, testable).

## 3. Non-goals

- Explicitly out of scope, to keep the change small.

## 4. Constraints

Which of the `docs/SPEC.md` §2 constitution rules apply / are at risk?

## 5. Design

How it works: data flow, new/changed packages, functions, signatures, endpoints,
UI. Keep it concrete enough to implement from.

## 6. Contracts / interfaces

New or changed public types, function signatures, HTTP routes, flags, env vars.

## 7. Acceptance criteria

Numbered, verifiable checks. Each maps to a test or a manual verification step.

1. ...

## 8. Test plan

Offline unit tests (fixtures) + manual verification against the cluster.

## 9. Rollout / docs

CHANGELOG entry, README section, memory update. Any migration or flag defaults.

## 10. Open questions
